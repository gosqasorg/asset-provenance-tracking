openapi: 3.0.0
info:
  title: Azure Functions API
  version: 1.0.0
  description: Auto-generated OpenAPI specification
servers:
  - url: http://localhost:7071/api
    description: Local development server
  - url: https://gosqasbe.azurewebsites.net/api
    description: Staging server
  - url: https://gdtprodbackend.azurewebsites.net/api
    description: Production server
paths:
  /getNewDeviceKey:
    get:
      tags:
        - GDT API
      summary: GET /getNewDeviceKey
      operationId: getNewDeviceKey
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
        '404':
          description: Not Found

  /createGroup:
    post:
      tags:
        - GDT API
      summary: Create a new group
      description: This endpoint creates a group and returns a unique group URL via multipart/form-data.
      operationId: createGroup
      requestBody:
        description: Group creation parameters and attachments
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - provenanceRecord
              properties:
                provenanceRecord:
                  type: object
                  description: JSON object containing the group metadata.
                  required:
                    - deviceName
                    - description
                  properties:
                    deviceName:
                      type: string
                      example: "Group Title"
                    description:
                      type: string
                      example: "Group Description"
                    number_of_children:
                      type: integer
                      example: 5
                attachments:
                  type: array
                  description: Optional binary files attached to the form data.
                  items:
                    type: string
                    format: binary      
      responses:
        '200':
          description: Successful creation
          content:
            application/json:
              schema:
                type: object
                properties:
                  groupUrl:
                    type: string
                    example: "{'groupUrl':'http://dev.gosqas.org/record/XuYCKWWDUEyv9vE5iL7Pzg'}"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: "Invalid input parameters"
  /createRecord:
    post:
      tags:
        - GDT API
      summary: Create a new record
      description: This endpoint creates a new record and returns a unique record URL.
      operationId: createRecord
      requestBody:
        description: Record creation parameters in postProvenance
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - provenanceRecord
              properties:
                provenanceRecord:
                  type: object
                  required:
                    - deviceName
                    - description
                    - children_key
                  properties:
                    deviceName:
                      type: string
                      example: "Record Title"
                    description:
                      type: string
                      example: "Record Description"
                    children_key:
                      type: string
                      description: Group key for grouped records, or empty string for standalone records.
                      example: ""
                attachment:
                  oneOf:
                    - type: string
                      example: ""
                    - type: object
                      required:
                        - file
                        - name
                      properties:
                        file:
                          type: string
                          description: Base64-encoded attachment content
                        name:
                          type: string
                          example: "photo.jpg"
      responses:
        '200':
          description: Successful creation
          content:
            application/json:
              schema:
                type: object
                properties:
                  recordUrl:
                    type: string
                    example: "{'recordUrl':'http://dev.gosqas.org/record/XuYCKWWDUEyv9vE5iL7Pzg'}"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: "Invalid input parameters"