import "./chunk-G3PMV62Z.js";

// node_modules/@urlpack/base-codec/lib/index.mjs
var g = (d) => {
  let n2 = d.length;
  if (n2 === 0 || n2 > 255) throw new Error("Invalid base alphabet length: " + n2);
  return { encode: (s) => {
    let i = 0, o = 0, l = [];
    for (let t of s) {
      i || t ? i = 1 : o++;
      let e2 = t;
      for (let r2 = 0; e2 || r2 < l.length; r2++) e2 += l[r2] << 8, l[r2] = e2 % n2, e2 = e2 / n2 | 0;
    }
    let a = o + l.length, c = Array(a);
    for (let t = 0; t < a; t++) c[t] = d[t < o ? 0 : l[a - t - 1]];
    return c.join("");
  } };
};
var p = (d) => {
  let n2 = d.length;
  if (n2 === 0 || n2 > 255) throw new Error("Invalid base alphabet length: " + n2);
  let s = new Map(Array.from(d, (i, o) => [i, o]));
  return { decode: (i) => {
    let o = 0, l = 0, a = [];
    for (let e2 of i) {
      let r2 = s.get(e2);
      if (r2 == null) throw new Error("Invalid character: " + e2);
      o || r2 ? o = 1 : l++;
      for (let f2 = 0; r2 || f2 < a.length; f2++) r2 += a[f2] * n2 >>> 0, a[f2] = r2 % 256, r2 = r2 / 256 | 0;
    }
    let c = l + a.length, t = new Uint8Array(c);
    for (let e2 = 0; e2 < c; e2++) t[e2] = e2 < l ? 0 : a[c - e2 - 1];
    return t;
  } };
};

// node_modules/@urlpack/base58/lib/index.mjs
var e = "123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz";
var r = g(e);
var m = r.encode;
var n = p(e);
var f = n.decode;
export {
  f as decode,
  m as encode
};
//# sourceMappingURL=@urlpack_base58.js.map
