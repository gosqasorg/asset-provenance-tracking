function e(r) {
  if (!r)
    return !1;
  const t = r.trim();
  return !t.startsWith("http://") && !t.startsWith("https://") ? !1 : t.replace(/^https?:\/\//, "").length > 0;
}
export {
  e as isUrl
};
