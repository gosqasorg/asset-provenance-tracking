import n from "pretty-bytes";
const o = (e) => {
  const t = Number.parseInt(
    e.headers?.["Content-Length"] || e.headers?.["content-length"] || "0",
    10
  );
  if (!(isNaN(t) || t <= 0))
    return n(t);
};
export {
  o as getContentLength
};
