import { getResolvedRef as t } from "@scalar/workspace-store/helpers/get-resolved-ref";
function i(...n) {
  for (const r of n)
    if (r) {
      if (!Array.isArray(r)) {
        const e = t(r);
        if (e?.url)
          return e;
        continue;
      }
      for (const e of r) {
        const o = t(e);
        if (o?.url)
          return o;
      }
    }
}
export {
  i as getFirstServer
};
